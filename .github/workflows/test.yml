name: Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: emteknetnz/gha-tag-release@main
        with:
          sha: beb684937d2bbed8046b5361003a3865d9437f76
          tag: mytag
          delete_existing: true
          release: true
          body: 'this is my body'
          generate_release_notes: false
