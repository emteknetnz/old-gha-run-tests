name: Test
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: emteknetnz/gha-tag-release@main
        with:
          sha: 288c2b732447c3397525734d9e04f6fbafe404fa
          tag: mytag
          delete_existing: true
          release: true
          body: >
            This is my body
            It has a few lines
            
            And a line break
            
            With "quotes" too
